mixin childCommentForm(comment = {}, postId, commentId)
	form.comment(action=`/p/${postId}/comments/${commentId}` method="POST" name="comment")
		h3 Add Comment
		textarea(name="text")
			if comment
				p= comment.text
		input(type="submit" value="Submit" class="button")