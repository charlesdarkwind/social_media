mixin childCommentForm(comment = {}, postId, parentId)
	form.comment(action=`/addComment/${postId}/${parentId}` method="POST" name="comment")
		h3 Add Comment
		textarea(name="text")
			if comment
				p= comment.text
		input(type="submit" value="Submit" class="button")